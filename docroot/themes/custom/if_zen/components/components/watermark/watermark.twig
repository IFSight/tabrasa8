{#
/**
 * @file
 * Component for watermark.
 *
 * Variables:
 * - modifier_class: [string] Classes to modify the default component styling.
 * - content: [string] The component contents.
 */
#}
{{ attach_library('if_zen/watermark') }}
<mark class="watermark {{modifier_class}}">
  {{content}}
</mark>
